name: device_security_checker
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  safe_device: ^1.1.1
  flutter_root_jailbreak: ^2.0.2
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
   plugin:
    platforms:
      android:
        package: com.example.device_security_checker
        pluginClass: DeviceSecurityCheckerPlugin
      ios:
        pluginClass: DeviceSecurityCheckerPlugin
